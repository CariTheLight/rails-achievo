<div class="container p-4 mb-5">
  <h1 class="fw-bold">Goals</h1>
  <% @goals.each do |goal| %>
    <%= link_to goal_path(goal) do %>
      <div class="goal-card">
        <h2 class="fs-4 fw-bold text-light"><%= goal.name %></h2>
        <p class="text-light"> <b>Start:</b> <%= goal.start_date %></p>
        <p class="text-light"><b>End:</b> <%= goal.end_date %></p>
        <div class="progress-group mb-3">
          <div class="progress">
            <div class="progress-bar" style="width: <%= goal.update_progress %>%;"></div>
          </div>
          <div class="percentage-show"><%= "#{goal.update_progress}%" %></div>
        </div>
      </div>
    <% end %>
  <% end %>
</div>