<div container>
  <h1>Edit Task</h1>
  <div class="form">
    <%= simple_form_for([@goal, @task]) do |f| %>
        <%= f.input :name %>
        <%= f.input :description %>
      <div class="actions" btn btn-primary>
        <%= f.submit 'Update Task' %>
      </div>
    <% end %>
  </div>
</div>
